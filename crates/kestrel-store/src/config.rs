//! kestrel.toml 单文件配置。
//!
//! 配置纪律（OpenHands 140+ 字段的反面教训，§2.2）：字段总数保持个位数
//! 到低两位数；每加一个字段先问能不能用约定代替。
//! 机组编制表（角色 -> 模型，或 auto）也在这里（§6.6）。

// TODO(M1): pub struct Config { backend_url, approval_policy, workdir, crew: CrewConfig, ... }
