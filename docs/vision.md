# 北极星：睡眠周期与自我进化

> 本文是方向宣言，不是当前设计承诺。当前架构见 [architecture.md](architecture.md)；
> 已定案的部分见 [ADR-0004](adr/0004-inverted-cost-model.md)。

## 一、前提判断

**本地模型终将追平今天的云端旗舰。** 这不是愿望，是趋势线：Qwen3-35B-A3B 已被社区
公认接近 8-12 个月前的边缘云模型，且差距每一代都在缩小。

推论：**当能力追平时，唯一的差异化是——谁的权重认识你。**
云端模型全球共享一套权重，它对你的了解永远只能是"笔记"（记忆文件、RAG）。
本地，权重就在你的硬盘上。

## 二、地基：成本模型反转（ADR-0004，已定案）

市面上每个 agent 都诞生于云端计费模式，DNA 里刻着"token 很贵，省着用"。
本地的真实成本结构是反的：

- **token 免费**——闲置 GPU 多生成一百万 token，电费以毛计
- **延迟才贵**——prefill 30 秒是本地体验的头号杀手

Kestrel 是第一个为"token 白菜价、延迟是黄金"设计的 agent。两个直接产物：

### 投机代理（Speculative Agency，M4）

投机解码在 token 层赌下一个词；我们在 agent 层赌用户的下一步。
用户还在读回复、还在打字时，空闲 slot 已在预计算最可能的几个未来
（"继续"、批准后的下一轮、"跑测试然后修"）。命中即秒回；赌错即丢弃——
token 免费，浪费是美德。把本地最大的耻辱（慢）打成卖点（比云端跟手）。

### 夜班（Night Shift，M5）

闲时 GPU 算力是 agent 的工资。用户不在时：蒸馏当日会话进长期记忆、
重建项目索引、给新模型跑能力探针、起草 TODO 修复草稿（只写草稿分支）。
早晨交一份夜班报告。

安全边界（OpenClaw 反面教材的三个否定）：默认只读、权限门全程管辖、
不监听任何网络端口。

## 三、北极星：睡眠周期（Dream Cycle，M6+）

完整的生物学隐喻，四阶段闭环：

| 阶段 | 行为 | 基础设施 |
| --- | --- | --- |
| 白天（工作） | 正常干活；权限门天然收集偏好数据——每次批准/拒绝是一个 DPO 偏好对，每次"编辑失败后用户改对"是一条 SFT 样本。所有 agent 都把这些扔掉了，我们存进事件日志 | 事件日志（已有） |
| 浅睡（整理） | 夜班：记忆蒸馏、索引、把当日会话挖掘成训练对 | 夜班（M5） |
| 深睡（做梦） | 用积累的偏好数据本地训练 LoRA 适配器。QLoRA 微调 8B 在 24G 显存内可行；llama.cpp 运行时挂载 LoRA，练完即插即用 | 模型池 + 闲时调度 |
| 醒来（考试上岗） | 新 LoRA 必须通过能力探针 + 回放测试的完整考试（工具调用可靠性不降、历史回归全绿）才获提升；不及格即丢弃。LoRA 是文件，秒级回滚，永不碰基座权重 | 能力探针 + 回放测试（M3） |

早晨的报告不再是"我做了什么"，而是：
"昨晚训练完成。新模型在你的代码库任务上工具调用成功率 87% -> 93%，
已通过 42 项回归考试，正式上岗。"

### 为什么这是结构性护城河

1. **用一个月，你的 agent 变成世界上独一无二的模型**——不是配置不同，是权重不同。
   LoRA 文件即"agent 的灵魂"，几百 MB，可携带、可备份、可分叉。
2. **云端在结构上永远跟不进**：共享权重不可能为单个用户训练
   （成本 + 隐私 + 安全审查三重死锁）。这不是功能差距，是商业模式的墙。
3. **骑在前提判断上**：本地模型越强，"权重认识你"的复利越高。
4. **让既有架构合龙**：事件日志=数据管道，权限门=偏好标注器，能力探针=考官，
   回放测试=考卷，夜班=训练时段，机组=展示舞台。没有一个部件是白设计的。

### 诚实的风险清单

- **偏好数据不平衡**：approve 远多于 deny；需要会话挖掘（失败-修复对、用户改写）补充。
- **灾难性遗忘**：考试闸门 + 秒级回滚兜底；基座权重永不修改；LoRA 按项目可分叉。
- **训练资源**：第一版只训 8B 副手（风险最小、显存最省），主脑后训；
  35B-A3B 的 MoE LoRA 可行性届时评估。
- **数据规模**：数周使用约产出数百到数千对——足够风格/约定/工具偏好级别的
  个性化，不追求能力级提升（那是基座模型的事）。

## 四、一句话

云端 agent 记笔记，Kestrel 做梦。你的每一次批准，都是一次梯度下降。
